# Root environment example (copy to .env and fill secrets)

# --- RPC / Solana ---
HELIUS_API_KEY=
ANCHOR_PROVIDER_URL=https://devnet.helius-rpc.com/?api-key=${HELIUS_API_KEY}
SOLANA_RPC_URL=
NEXT_PUBLIC_SOLANA_CLUSTER=devnet

# Program IDs (public)
SOLANA_PROGRAM_ID=5g75VtkCiComNnhTtCjukVZ67peJosfbbvygMoFBGKXB
NEXT_PUBLIC_SOLANA_PROGRAM_ID=5g75VtkCiComNnhTtCjukVZ67peJosfbbvygMoFBGKXB

# Treasury wallet (public)
TREASURY_WALLET=

# Backend Database
MONGODB_URI=
PORT=10000
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Optional: Program private key (DO NOT COMMIT). Example format only:
# PROGRAM_PVT_KEY=[1,2,3,...]

# Notes:
# 1. Keep this file tracked; your actual .env stays untracked.
# 2. Anchor will read ANCHOR_PROVIDER_URL via ${} environment expansion if exported before commands.
# 3. Set HELIUS_API_KEY then run: export ANCHOR_PROVIDER_URL="https://devnet.helius-rpc.com/?api-key=$HELIUS_API_KEY"