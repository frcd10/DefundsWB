// Deprecated: SwapPanel has been removed in favor of the dedicated /swap page.
// This file intentionally left as a no-op to avoid stale import errors during incremental builds.
export function SwapPanel() {
  return null;
}
